spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev  #开发环境
  application:
    name: microservicecloud-config-atguigu-dev
---  #没有这个横线 就会报红
spring:
  profiles: dev  #测试环境
  application:
    name: microservicecloud-config-atguigu-test
  
#必须是utf-8
